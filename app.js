"use strict";

let dayTrip = ("Houston, Las Vegas, San Diego");

    function shuffleArray(dayTrip){

        let Houston = 0 
        let LasVegas = 1 
        let SanDiego = 2 
        return Math.floor(math.Random() * 3);
    }

        console.log(itworks);